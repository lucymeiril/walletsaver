# 재개점 — pass82

- 최신 인증 사본: `.debug-artifacts/initial-catalog-20260915-pass82/checks-passed.json`. 공개 승인 전 단계이며 운영 DB에 적용하지 않았다. pass78/79/80은 신규 규격 충돌을 발견한 실패 실행이므로 기준점으로 쓰지 않는다.
- 원본 9,196건 중 6,587건 적재, 2,609건 미해결. 상품군 4,288, 규격 4,318, 판매 페이지 4,438, 가격 관측 6,587, 매칭 규칙 4,365. 카테고리 565, 키워드 455, 원본 카테고리 매핑 348.
- pass75에서 이마트 `커피/원두/차` 34건을 전수 검토했다. 명확한 28건을 커피음료·커피믹스·인스턴트커피·홍차·녹차/말차·허브차·곡물차·보리차·콤부차·과일청차로 분리했다.
- 보류 6건: 혼합 차 선물세트 3건, `120X14G`가 14g으로 오염 저장된 아이스티, 실제 용량 없는 레몬즙 14입, 종류가 빠진 `오리지널 리필 170g`. 좁은 독립 근거 없이 복구하거나 추정하지 않는다.
- pass76에서 오염된 코스트코 `고기` 52건을 전수 검토했다. 공식 상품 주소가 형태를 확인해 주는 혼합향신료·모둠채소·해초샐러드·반려견 냉동화식 2종만 분리해 5건을 추가했다. 이를 위해 4단계 리프 `혼합향신료`, `모둠채소`를 추가했다. 그릴/숯/가전, 여러 음식 혼합세트, 기존 충돌 장치가 막은 밀키트는 보류했다.
- pass77에서 pass71 이후 남은 코스트코 `커피` 51건을 검토했다. 공식 URL과 명시 형태로 파드커피·분말라떼 3종·병입 아이스티의 종류를 확정했다. 파드커피와 토피넛라떼는 수량 문제가 남아 분류만 되고 사본에는 미적재; 우베라떼·피스타치오라떼·아이스티 3건만 실제 추가됐다. 장비·선물세트·세척캡슐은 보류했다.
- pass81에서 코스트코 `과자` 잔여 43건을 검토했다. 명시 형태와 공식 URL이 일치한 건고구마·과자·채소/해조/곡물스낵·속채운빵·양갱·모나카·사과 13형태를 분류했고 11건을 실제 적재했다. 비비고칩과 우유앙빵은 수량 문제로 분류만 유지했다. 두 꿀오랑과 2겹 묶음 떡빵은 실제 재수집 규격 충돌이 나 자동 대상에서 제외했다.
- pass82에서 코스트코 `세제` 잔여 38건을 전수 검토했다. 명시 형태와 공식 URL이 일치한 캡슐세제 5종·액체세제 4종·향기부스터 1종을 분류했고 8건을 실제 적재했다. 파워브라이트 2종은 수량 문제로 분류만 유지했다. 혼합 선물세트·도매 묶음·베이킹소다/용기 결합·의료/가전 오염 행은 보류했다. 새 4단계 리프 `세탁향기부스터`를 추가했다.
- 검증: 관련 테스트 1,642개 + 수집기 매칭 79개 통과. 실제 신규 8건 재수집 경로 hit, 총 24개 stale-key/context 변형 거부. 기존 119상품군 375 hit/25 miss, 경로별 1,200변형, 멱등성·원본 보존·431개 명시 결정 보존 통과.
- 보호 해시: 운영 DB `ef1dbdae18c371ae9402202e2fee9be2396e7c9a6807f6e8e47eaded0908b0d2`; 원본 파일 `1406daf131add703d5dac0021d9c6f5194823ad59aef66bd3869f402907bf7e5`.
- 다음 작업: 이마트 `친환경/유기농` 38개 잔여 미분류 원문을 검토한다. 이미 처리한 신선농산물은 중복 검토하지 않고, 제목으로 최하위 식품 형태가 분명한 추가 품목만 분류한다. 행사 모음·용량뿐인 이름·비식품·혼합팩은 보류한다.
- 항상 `AGENTS.md`와 `docs/catalog-state.json`을 읽고 하네스 preflight 후 새 run-id로 실행한다. 운영 DB SQLite 접근, 승인·공개·초기화·삭제는 금지한다.
