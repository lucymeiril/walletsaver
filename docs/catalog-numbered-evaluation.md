# 번호 분류 실사용 평가 — pass100

- 대상: 이마트 채소·델리·과일, 홈플러스 액상차. 고유 제목 45개 / 원본 관측 53개.
- 최종 결정: 분류 규칙 14제목, 보류 31제목. 실제 추가는 14관측 / 11상품군·판매 페이지. 미해결 2,330 → 2,316.
- 신규 재수집 14 hit, 변형 입력 42개 거부. 관련 검사 2,009개 + 수집기 79개 통과. 운영 DB·원본 보존. 인증: pass100-numbered-eval.
- 자동 생성한 결정 JSON 4개는 총 21,002자. 상품별 필드·제목·해시를 모델이 다시 작성하지 않았고 선반별 Python 모듈·연결 함수를 추가하지 않았다. 이는 생성 파일 크기이며 토큰 절감량 측정은 아니다.
- 성공 하네스 실행은 69.43초, 전체 단계 로그는 6,010바이트였다. 화면에는 단계 경로와 최종 요약만 반환했다.

## pass103 실사용 평가 (2026-09-17)

- 생활용품 19종 검토: 8종 리프 지정, 혼합 구성·용도 확인 등 11종 보류. 청소솔·물기제거기·청소용구연산·행주전용비누 리프 추가. 번호 규칙의 공통 매개변수 검사가 실제 분류 및 잘못된 마트/경로/변경명/혼합명 거부를 검사했다.
- 사본 추가는 관측 ID 3개 / 상품군 3개 / 판매 페이지 3개. 미해결 2,296. 나머지 분류된 5종은 report에서 실제 unit_unresolved 확인. 분류 성공을 적재 성공으로 세지 않는다.
- 신규 재수집 3 hit, 0 miss; 경로당 변형 9개 거부. 원본과 운영 DB 보존, 공개 미승인. 인증 pass103.
- 단계 합계 70.02초: 관련 검사 10.09초, 수집기 검사 6.70초, 빌드 37.67초. 성공 로그는 파일에 저장. 토큰 절감률은 측정하지 않았다.
- 시간 프로파일 결함 발견: 실제 JUnit XML에서 2,039개 case의 classname이 빈 문자열이다. 검사 통과에는 영향 없지만 모듈별 비용 분석으로 쓸 수 없다. 다음 계측 보완 시 실제 XML의 빈 classname 사례를 회귀검사에 넣고 파일 경로 식별을 확보할 것. 이 결과로 검사 생략을 결정하지 않는다.
- 번호 입력으로 제목/해시 재작성은 피했지만, 후보 선반 선택은 별도 조회가 필요했다. 다음 도구 개선은 후보별 미검토 수와 규격 미확인 수를 짧게 보여주는 목록 기능이 우선이며, 수량을 추정해 적재율을 높이지 않는다.
- checkpoint는 한 번 전진한 뒤 같은 run의 다음 작업 문구만 갱신하려고 다시 호출해도 Unexpected baseline으로 거부한다. 첫 갱신은 정상 보존되었다. 이번 후속 메모는 두 문서의 next_task만 수동 동기화했으며, DB·인증서를 바꾸지 않았다. 다음 개선 후보는 재적용과 메모 갱신을 분리하는 것이다.

## 확인한 한계와 보완

## pass110 조건부 합격 평가 (2026-09-19)

- 캡슐세제 보류1종 재검토 + 주방32종 검토. 신규16상품/16판매 페이지/30관측, 미해결2,234. 실제 재수집30 hit/0 miss, 변형 입력 거부 검사 통과. 원본/운영 DB 보존.
- 다회용 투명컵3종의 ml를 내용물 양이 아닌 용기 용량으로 처리하고 판매 개수를 사용했다. 기존 종이컵의 엄격한 수량·표시 충돌 검사를 공유하며 뚜껑/혼합/추가 팩/수량 누락 음성 검사를 추가했다. 6관측 적재·재수집 확인. 볶음스푼/랩/호일3종은 실제 unit_unresolved로 미적재.
- pass109 검사 실패는 기존 캡슐세제070137671의 무조건 보류 기대값이었다. 정확 문맥의 경로 검토가 추가된 계약에 맞춰 갱신하고, 명시 검토가 없으면 원래 충돌이 복원되는 대체 검사를 추가했다. 실패 로그는 보존하고 pass110 새 실행 인증.
- 현재 기준본의 보류 초안을 `revise-path --draft`로 검토할 수 있게 해 보류 인증용 중간 빌드가 필요 없는 경로를 추가했다. 명시 옵션/기준본 일치/인증된 파일 해시 우회 방지 단위검사 완료. 실제 초안 보정 묶음에서는 아직 미평가이므로 다음 실사용에서 확인할 것.
- 평가: 반복 필드 작성·후보 집계·결과 조회·재개 문서 갱신 자동화와 제한된 경로 보정의 안전성은 실사용 근거가 있다. 분류 하네스로 조건부 합격. 모델 토큰 사용량의 동일 조건 전후 비교가 없으므로 토큰 절감률 또는 모델 판단 성능 향상까지 입증했다고 말하지 않는다.

## pass107–108 경로 충돌 재검토 (2026-09-19)

- `revise-path`는 인증된 보류만 해시/원본 문맥 확인 후 수정하고, 이전 JSON·해시·보류 사유·새 판단 이유를 보존한다. 실제 명칭상 욕실세정제가 아닌 9종, 섬유유연제가 아닌 탈취제3종에 적용했다.
- 기존 제품형태 검사가 이미 부정한 경로 후보만 제외한다. 정상 후보 및 이름/URL 충돌은 유지하는 양성·음성 검사, 변경 마트/경로/혼합 상품명 검사를 추가했다. 분류 결과에 제외한 경로 리프도 남긴다. 검토는 분류 규칙 제안이며 DB/공개 승인이 아니다.
- pass107: 12상품17관측 추가, 재수집17 hit/0 miss. pass108: 남은 세제13종 검토해6상품7관측 추가, 재수집5 hit/2 miss. 후자의 변기세정제2관측은 실제 report에서 promotion_unresolved/pending_review 확인. 가격 조건을 추정해 활성화하지 않았다.
- 총18상품/18판매 페이지/24관측 추가, 미해결2,264. 원본/운영 DB 및431결정 보존. 양쪽 실행 모두 checks-passed 인증.
- 남은 도구 불편: 최초 검토에서 발견한 경로 문제도 현재는 보류 인증 후 revise-path가 필요하다. 초안에 같은 검증을 적용하는 경로가 다음 개선 후보다. hp-liquid-sep19의 캡슐세제1종이 실제 후속 대상이다. 검사 생략/캐시는 도입하지 않았다.

## pass105–106 실사용 평가 (2026-09-19)

- `shelves --contains 세제`로 별도 임시 집계 없이 후보를 골랐다. 번호 검토/보류 원본 ID를 제외하며 고유 제목과 관측 수를 구분한다. 5개 선반 46종 중 7종 적재, 39종 보류(12종 원본 경로 충돌 포함). 총 7상품/7판매 페이지/8관측 추가, 미해결 2,288. 홈플러스 동일 제목의 두 관측은 상품 하나로 들어갔다.
- pass104는 원본 카테고리와 용도 충돌 12종 때문에 검사 단계에서 실패했다. 규칙을 약화하지 않고 원본 초안을 백업해 보류했다. 실패 로그를 보존하고 pass105/106에서 새로 인증했다. 두 실행의 신규 재수집은 각각4 hit/0 miss, 변형 입력 거부 검증 통과. 원본/운영 DB 보존, 공개 미승인.
- xunit1의 file 속성을 사용해 실제 XML의 빈 classname 문제를 해결했다. pass105에서 8개 검사 파일이 모두 구분된다. taxonomy 1,235 case의 누적 시간은1.492초였다(전체 pytest 프로세스 시간과 다름). 검사 생략이나 캐시는 추가하지 않았다.
- checkpoint 동일 호출은 무변경, 메모 갱신은 별도 백업으로 실제 검증했다. 첫 실사용에서 상태 문서 해시가 바뀌는 문제를 추가 발견했으며, 최초 백업 해시 및 보호 설정을 검증한 뒤 상태 문서만 코드 해시 비교에서 분리했다. 코드/DB 해시 검증은 유지한다.
- 도구 단독 검사의 기존 import 경로 의존성을 수정했다. 입력 재작성·임시 집계·재개 메모 수동 수정은 줄었지만 토큰 절감률은 측정하지 않았다. 남은 큰 병목은 목록 출력이 아니라 카테고리 충돌을 명시적으로 검수해 반영할 경로와 규격 근거다. 이를 자동 규칙으로 우회해서는 안 된다.

- 기존 보류 계약은 번호 도구가 자동 수집하지 않는다. 과일 가변 중량 계약을 늦게 발견해 pass99가 테스트 단계에서 실패했다. 로그를 보존했고 새 pass100으로 인증했다. 다음 후보 선택 때 해당 선반의 기존 테스트를 먼저 검색한다.
- 장조림 2건은 원재료 분류와 완제품 분류가 충돌했다. 숫자만으로 초안을 보류하고 백업하는 hold-draft 명령을 추가했다. 인증 기준본에 들어간 파일은 수정 거부한다.
- 보류를 다시 만들 필요가 없어 다음 prepare 목록에서 제외된다. 근거 보강 시에는 명시적으로 재검토해야 한다.
- 분류 규칙 통과와 실제 적재는 다르다. 이번 미적재 원문 중 새송이·만가닥은 unit_unresolved, 팽이버섯은 적재 보고서의 assignment_missing 계열 사유다. 팽이버섯의 분류 JSON은 리프를 갖고 있어 보고서 차이의 원인은 후속 확인 대상으로 남긴다.
- 판단 자체가 필요한 애매한 상품, 새 리프 설계, 기존 충돌 계약 확인은 자동화되지 않았다. 전체 재구축 비용도 유지한다.

결론: 반복적인 출력·필드 작성에는 유용하고 기존 검사도 실제 오류를 잡았다. 분류 판단 속도나 전체 토큰 절감률까지 입증한 것은 아니다.
