# 현재 사본 하나 + 압축 이력

- 현재 DB/분류 결과는 `docs/catalog-state.json`의 baseline 하나만 본다. pass 번호는 식별자이므로 재사용하거나 2부터 다시 시작하지 않는다.
- 사용자 승인 보관 정책(2026-09-20): 새 실행 인증과 checkpoint 갱신 뒤 `py tools/catalog_archive.py`로 범위를 확인하고 `--apply`로 이전 initial-catalog-*pass* 폴더를 압축 이력으로 전환한다. 실패 기록도 보존한다.
- 도구는 최신 baseline/원본/운영 DB를 제외한다. SHA256별 파일 내용을 ZIP 안에 한 번씩 저장하고 모든 압축 내용을 다시 해시 검증한다. 원본 파일 변화와 preflight를 확인한 뒤 개별 파일만 삭제한다. 기존 ZIP은 덮어쓰지 않는다.
- `.debug-artifacts/catalog-archives/*-history.zip`의 `manifest.json`은 원래 상대 경로 → SHA256 → 크기 목록이다. 실제 내용은 `blobs/<SHA256>`에 있다. 과거 자료가 필요하면 해당 항목만 별도 검토 폴더로 추출하고 해시 확인한다. 과거 pass 폴더를 새 기준본으로 복원하지 않는다.
- 과거 인증서는 당시 결과의 기록이다. 현재 코드/현재 DB의 인증이 아니며, 보관 후 기존 파일 경로는 존재하지 않을 수 있다.
- Git의 numbered_reviews, 기존431개 명시 결정, 외부 AI 제안 및 handoff 폴더는 이동·자동승인하지 않는다. 보류/제안을 중복으로 간주해 버리지 않는다.
- 2026-09-20 pass2~100 정리에서 재생성 DB/HTML은 이미 삭제됐다. 당시 동일 JSON의 보존 위치는 `.debug-artifacts/archive-cleanup-20260920/cleanup-manifest.json`에 있다. 그 위치가 지금 압축됐다면 ZIP manifest에서 해당 원래 경로를 찾는다.

## 다음 검토 우선순위

- 이번 보관 결과: 과거579파일을 ZIP2개(합계약694MiB)로 검증 보관, 추가약7.28GiB 확보. `pass38b` 같은 예전 문자 접미사도 포함했다. live pass 폴더는 pass113 하나다. 같은 기준본의 추가 보관은 새 ZIP을 생성하며 기존 ZIP을 덮어쓰지 않는다.

- pass113 미해결2,228건. 분류 후보 없음1,925건, 수량 미해결549건, 프로모션 미해결195건, 복수 수량142건(사유 중복 포함, 합계 아님).
- 번호 검토35묶음: 리프 지정94종, 보류118종. 전체 역사/외부 제안 전체를 센 숫자가 아니다.
- pass111에서 백2종/장갑1종, 총3상품6관측 추가. pass112 실패 후 pass113에서 조리도구7종 분류 인증; 전부 unit_unresolved, 일부 promotion_unresolved라 적재 증가는0.
- 남은 선반만 계속 훑기보다 `report`로 분류됐으나 미적재인 수량 유형을 묶어 검토한다. 국물팩50매입, 랩/호일의 폭×길이, 판매 개수 없는 조리도구는 서로 다른 문제다. 임의로1개로 가정하지 않는다.
- 외부 제안/보류를 최신 결과와 합칠 때는 원본 ID·마트·전체 경로·상품명으로 확인하고, 기존 원본/규칙을 보존한 채 새 실행에서 인증한다. 파일 이름이나 완료 주장으로 승인하지 않는다.
