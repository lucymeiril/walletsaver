# 현재 상태 — pass45 / 2026-09-14

## 제안 작업 인계

- `fix-external-review-errors / 기준a752db5 / external-review 제목·판매키 불일치 4결정 / 제안 완료 / proposals/fix-external-review-errors.json / 다음: fix-legacy-177-192`. 토닉워터 후속 관측 ID 2쌍을 판매키별로 바로잡고, 명란마요에 잘못 묶인 소시지치즈 후속 관측을 올바른 판매키로 재연결해 4개 오류를 5개 현재형식 결정으로 정규화했다.
- `fix-legacy-177-192 / 기준a752db5 / pass45 remaining 177-192의 현재 잔존 ID / 제안 완료 / proposals/fix-legacy-177-192.json / 다음: fix-legacy-445-451`. 이미 적재돼 remaining에서 사라진 185·188·191은 제외했고, 180·181·182도 현재 남은 ID만 포함했다. 구식 `hold+suggested_new_leaf` 중 제품형태가 명확하고 리프 부재만 원인인 항목은 `new_leaf_needed`로 정규화했다.
- `fix-legacy-445-451 / 기준a752db5 / pass45 remaining 445-451 / 제안 완료 / proposals/fix-legacy-445-451.json / 다음: 남은 기존리프·신규리프 제안 큐`. 이전 결과파일은 원격에 없었지만 현재 7항목의 원문과 이전 참조 제안을 대조해 현재 형식으로 모았다.
- 이번 제안작성에서는 DB/accepted/REMAINING/archives를 수정하지 않았고 테스트·lint·import도 실행하지 않았다. 세 결과파일의 `executed_tests`는 모두 빈 배열이다.
- 이미 적재된 ID의 옛 형식 재정리는 새 DB 진행량으로 계산하지 않는다. 새 리프가 필요한 결정은 설계 검토 대상이며 자동 반영/완료 처리하지 않는다. 과거 순회 지점을 전체 미검토 목록으로 해석하지 않는다.

## 실제 반영

- 사본 `.debug-artifacts/initial-catalog-20260914-pass45`: **5,855적재 / 3,341미해결**, 상품군3,688 / 규격3,718 / 판매페이지3,838 / 매칭규칙3,765. 사용 카테고리466 / 키워드380(기존 정의 중 사용된 항목 증가, 새 분류 설계 없음).
- 이번97판매상품/178관측 분류근거 등록 중 **87상품군·판매페이지/160관측 새 적재**. 10상품/18관측은 기존 충돌검사로 계속 보류. 수량/할인 규칙은 변경하지 않았다. 행사보류787관측은 비교금지 유지.
- 기존 포함행/수동431결정 보존, 원본SHA 불변, 이번 검토ID 외 새 적재 없음, 두 번째 import 멱등성 통과. 관련733테스트·DB무결성·리프검사·매칭119상품군/375hit·25miss/경로별1,200변형 통과. 운영 승인/공개 없음. 전체 앱/라이브수집/브라우저 미실행.

## 외부 작업 평가

- `32d158b..0484e54`의 제안78파일 감사. 현재형식488결정/871고유ID, 수정된 파일들끼리 중복ID0·상충결정0. 구식/비결정 항목25개는 별도 보관하고 자동 승격에서 제외했다. 전체 과거 제안까지 무충돌임을 뜻하지 않는다.
- 마트 누락은 개선됐다. 제목 불일치4결정(판매키도 불일치3)은 `proposals/fix-external-review-errors.json`에서 현재 원문/ID 기준으로 교정했다. 토닉워터 2건은 후속 관측 ID가 서로 뒤바뀐 문제였고, 명란마요김밥은 소시지치즈 후속 관측이 잘못 묶인 문제였으며, 핫도그는 정규화된 source_title 공백 차이를 맞췄다. 과거 제안 파일은 이력으로 유지했다.
- 현재 미적재가 아닌156관측은 모두 pass44에 이미 적재된 ID였다. 해당 옛 제안의 형식 재정리는 새 DB 진행량이 아니다. 기존 정의로의 제안을 DB-ready로 부르는 추가 지침은 실제 충돌/단위검사와 혼동되므로 용어를 정리했다.
- 원본마트/리프별 SHA256 순서2개씩164표본을 검토했다. 메밀면/냉면·찐만두·양념육·어종·과즙음료·젤리·탈취제 등 불명확한 형태는 승격 제외. 롯데마트 후보와 신규리프/보류 판단도 이번 승격 제외. **전량 의미검증이나 전체 제안 합격 아님.**
- 옛 문서와 proposals는 이력으로 남겼다. 최신 링크만 이 폴더로 이동. 관련 검사만 실행하고 통합·백업은 이번 묶음 전체에 한 번 수행했다.
