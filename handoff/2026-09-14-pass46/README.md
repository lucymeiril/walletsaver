# 현재 재개점 — pass46-final / 로컬 통합 방식

GitHub 채팅 외주를 반복하는 대신 로컬에서 공통 분류 규칙을 구현·검증한다. 과거 작업 지침의 전량 문서 정리·외주용 자료 복제는 재개하지 않는다.

- 최신 사본: `.debug-artifacts/initial-catalog-20260914-pass46-final`. **5,990적재 / 3,206미해결**. pass45 대비78상품군/판매페이지·135관측 증가. 분류 해결 자체는89판매페이지/150관측이며 나머지는 용량 등 적재 조건으로 보류.
- 기존 사본/원본과431결정 보존. 운영 DB 및 복원 수집DB의 파일 SHA256까지 작업 전후 동일 확인. 운영 승인·공개 없음. 초안 `.debug-artifacts/initial-catalog-20260914-pass46`은 혼합 단무지+우엉 반례 수정 전이므로 사용 금지.
- 신규18리프 정의: 육포·껌·누룽지·깍두기·단무지·족발·치즈스틱·또띠아·냉동감자튀김·고무/니트릴장갑·수세미·행주·샤워볼·목욕타월·수건·식품보관백·조리집게. `initial_product_forms.py`는 상품명+문맥으로만 후보를 만들며, 기존 후보가 있는 상품에는 적용하지 않는다. 기존 충돌/가격/단위 검사를 해제하지 않았다.
- 관련766테스트, DB/FK/리프/791행사보류 비교금지, 이전 포함행·수동431결정·멱등import 검증 통과. 실제 매칭119상품군/375hit·25miss/경로별1,200변형 통과. 전체 앱·라이브수집 검사는 이번 미실행.
- Luna 1개를 읽기 전용 JSON 집계에만 사용했다. DB/파일쓰기/git/네트워크 금지 범위로 지시했고 코드·DB 구현은 주 에이전트가 수행했다. 모델별 실제 할당량 절감률은 측정하지 않았으며 절감 효과를 확정하지 않는다. 다음은 읽기 전용 집계를 반복하지 말고 이번 집계를 재사용한다.

## 데이터와 다음 작업

[delta.json](delta.json)의135개 ID를 기존 `../2026-09-14-pass45/remaining/`에서 빼면 현재 미적재 목록이다. 이는 작은 차이 자료이며 새로운 전체 DB 압축본은 아니다. 완전한 최신 unresolved는 로컬 최종 catalog-bundle.json에 있다. 코드의 taxonomy_categories()가 최신18리프를 포함하며 pass45 taxonomy.json은 이전 버전이다.

Luna 집계: 제안 중 실제 남은 ID와 연결된 신규리프 요청322관측/95후보(중복제거). 상위는 육포·수건·과일청차·과일농축액·냉동감자·누룽지·또띠아·식품보관백·삼계재료·조리집게. 이번 해결 항목을 제외하고 과일청/농축액/삼계재료 및 그 밖의 빈도 높은 형태를 묶어 검토한다. 후보 수는 실제 반영 완료 수가 아니다.

하위 에이전트에는 읽기 가능한 자료·쓸 수 있는 출력 파일을 명시하고 원본 `.walletsavior`와 모든 SQLite 접근을 금지한다. 필요하다면 계정정보 없는 입력 사본만 제공한다. 지시문은 OS 접근통제와 같지 않으므로 원본 파일 해시/작업 diff를 주 에이전트가 확인한다. 원본에서 직접 작업하지 말고 복원된 수집전용 사본으로 새 출력 폴더를 만든다.

검증: `tools/verify_product_form_batch.py`, `tools/verify_initial_stage.py initial-catalog-20260914-pass46-final`, `tools/verify_reviewed_runtime.py initial-catalog-20260914-pass46-final`. source fixture와 이전 DB를 덮어쓰지 않는다.

마지막 동기화에서 원격0091915까지의 pass45 전체 제안/오류수정 파일과 완료 주장을 보존했다. 이는 이번18리프 구현과 별개이며 미검증이다. complete-remaining-001-451.json을 실행결과로 신뢰하거나 전량 DB 승인하지 않는다. 과거 예약의 최신 내용은 pass45 CURRENT_STATUS를 참조하되 이번 DB 실적은 위 수치를 따른다.
