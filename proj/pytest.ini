[pytest]
testpaths = tests engine/tests crawlers storage/tests api/tests scheduler/tests
asyncio_mode = auto
markers =
    unit: 단위 테스트
    integration: 통합 테스트
    slow: 느린 테스트 (네트워크 등)
addopts = -v --tb=short
