# WalletSavior rd2-ai-provider-audit 결과

**감사 ID**: `a3a7f209`  
**실행 일시**: `20260518-013859`

---

## 1. 직전 run-bd82105d 적대적 진단

### 결론: **circumstantially_live**

| 항목 | 값 |
|------|-----|
| run_id | `bd82105d` |
| dry_run | `False` |
| 총 라벨링 건수 | 755건 |
| 총 배치 수 | 39배치 |
| 총 shrink 호출 수 | 59회 |
| ok 호출 수 | **49회** |
| retryable 에러 수 | **10회** |
| wire log 존재 | **False ← 없음** |
| product_matches_before | 0건 (0=캐시 비운 상태) |
| keyword_alias_before | 0건 |
| 캐시 우회 확인 | `True` |

### 직전 run 판정: 실 호출 **간접 증거 있음** — 결정적 증거 없음

> **retryable_errors = 10건**: fixture/mock은 절대 retryable error를 발생시키지 않는다.
> 이것은 실 API 호출의 간접 증거이지만, wire-level HTTP 로그가 없어 **결정적 증거가 아니다**.
>
> 직전 run에 wire-level HTTP 로그가 없음. shrink_log는 ok/retryable 카운트만 제공하며 HTTP URL/latency/status 없음. retryable_errors > 0은 실 API 호출의 간접 증거이지만 결정적 증거가 아님.

**타이밍 분석**: Run elapsed ~20 min (00:22→00:42). 39 batches × 12s rate limit ≈ 8 min. Remaining time: crawling overhead. Timing consistent with live API calls.

---

## 2. 재실행 100건 결과 (wire log 강제 활성화)

| 항목 | 값 |
|------|-----|
| 샘플 크기 | None건 |
| 소요 시간 | None초 |
| 크롤러 행 | None건 |
| ingested 행 | None건 |
| AI 제안 수 | None건 |
| 게이트 통과 | None건 |
| 배치 수 | None배치 |
| ok 호출 | None회 |
| retryable | None회 |
| quota 소진 | None |
| pipeline 오류 | None |

---

## 3. Wire Log 분석 — 결정적 증거

**Wire log 경로**: `E:\pdf\capston01\.walletsavior-live-validation\rd2-ai-audit\provider-wire-log-20260518-013859.jsonl`

### 결론: **NO_CALLS_CAPTURED**

| 항목 | 값 |
|------|-----|
| 총 HTTP 요청 수 | **0건** |
| 성공 (2xx) | 0건 |
| 실패 | 0건 |
| Google GenAI API 호출 | **0건** |
| 도메인 목록 |  |
| 평균 latency | None ms |
| 최소 latency | None ms |
| 최대 latency | None ms |

⚠️ **Wire log 샘플 없음** — HTTP 호출이 캡처되지 않았습니다.

---

## 4. 최종 감사 결론

### ❌ 실 API 호출 증거 없음

- Wire log에 `generativelanguage.googleapis.com` 호출 0건
- wire logger가 실제로 httpx client에 부착되지 않았거나 API key 누락으로 호출 자체가 없었음
- 반드시 `WALLETSAVIOR_WIRE_LOG_PATH`와 API key 설정 확인 필요

---
*자동 생성: `20260518-013859` | audit_id `a3a7f209`*